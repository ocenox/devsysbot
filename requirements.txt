# DevSysBot runtime dependencies.
# The CLI degrades gracefully: it falls back from the Textual TUI to a rich/questionary
# flow to plain stdlib input(), and skips Claude refinement if anthropic / the API key
# are absent. The logo falls back from the PNG (rich-pixels+Pillow) to ASCII to a wordmark.
textual>=0.60
rich-pixels>=3.0
pillow>=10.0
questionary>=2.0
rich>=13.0
anthropic>=0.40
